import { ClassificationPipe } from './classification.pipe';

describe('ClassificationPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassificationPipe();
    expect(pipe).toBeTruthy();
  });
});
