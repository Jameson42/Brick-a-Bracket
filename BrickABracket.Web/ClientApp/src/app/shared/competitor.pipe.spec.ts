import { CompetitorPipe } from './competitor.pipe';

describe('CompetitorPipe', () => {
  it('create an instance', () => {
    const pipe = new CompetitorPipe();
    expect(pipe).toBeTruthy();
  });
});
