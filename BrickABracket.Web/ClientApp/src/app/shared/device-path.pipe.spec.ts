import { DevicePathPipe } from './device-path.pipe';

describe('DevicePathPipe', () => {
  it('create an instance', () => {
    const pipe = new DevicePathPipe();
    expect(pipe).toBeTruthy();
  });
});
